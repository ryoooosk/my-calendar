import SchedulesContainer from '@/components/pages/schedules/schedules-container';
import { View } from 'react-native';

export default function SchedulesPage() {
  return (
    <View className="flex-1">
      <SchedulesContainer />
    </View>
  );
}
