import SchedulesContainer from '@/components/pages/Schedules';
import { View } from 'react-native';

export default function SchedulesPage() {
  return (
    <View className="flex-1">
      <SchedulesContainer />
    </View>
  );
}
